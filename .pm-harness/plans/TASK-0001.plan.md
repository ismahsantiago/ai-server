---
task_ref: TASK-0001
category: complex
status: approved
created: 2026-07-23
created_by: engineering-manager
approved_by: pm-orchestrator
approved_at: 2026-07-23T23:31:31Z
---

## Objective
Establish a reproducible Linux + Docker foundation for the local AI lab: baseline runtime checks, folder skeleton, first local model-serving path, and smoke benchmark evidence under 12 GB RAM constraints.

## Todos
- [x] Create lab skeleton (models/, datasets/, experiments/, logs/, backups/, scripts/, docs/) and AC: directories exist with README notes for intended contents.
- [x] Define Docker-first baseline (compose or equivalent) for at least one local model runtime and AC: service can start and report healthy status.
- [x] Expose a local API-compatible endpoint profile and AC: one sample request returns a valid response payload.
- [x] Implement balanced resource preset (medium-fast/medium/good) and AC: config captures CPU/RAM limits and startup flags.
- [x] Run and store smoke benchmark (latency + memory footprint) and AC: report file exists in logs/benchmarks with timestamp and model/runtime metadata.
- [x] Document LAN-safe enablement path and AC: runbook includes localhost default, LAN opt-in, auth requirement, and firewall note.
- [x] Align Sprint 1 legacy root artifacts with generator-first flow and AC: root docs/scripts clearly point to clone -> matrix -> generate -> validate -> start as canonical path. (amendment 2)
- [x] Add compatibility note for legacy files and AC: docker-compose/profile/script root files are labeled compatibility/examples and reference equivalent generated paths. (amendment 2)
- [x] Verify generator-driven replacement path and AC: at least one preset generation + validation run proves Sprint 1 outcomes are reproducible via generated workspace. (amendment 2)
- [x] Update TASK-0001 changelog entry and AC: Unreleased section references TASK-0001 generator-first closure alignment. (amendment 2)

## Gates
- `docker --version`
- `python3 --version`
- `python3 -m pip --version`
- `test -f .pm-harness/HARNESS-SPEC.md`
- `python3 .pm-harness/bin/harness.py validate`
- `python3 .pm-harness/bin/harness.py plan check TASK-0001`

## Risks
- Linux distro/package differences may break Docker installation assumptions; mitigate by documenting distro-specific prerequisites and fallback commands.
- 12 GB RAM can cause host instability during model load; mitigate with conservative default limits and one-model-at-a-time startup profile.
- LAN enablement can unintentionally expose the service; mitigate with localhost default, explicit opt-in, token auth requirement, and firewall instructions.
- Runtime/model mismatch (unsupported quantization format) can block serving; mitigate by selecting at least one known-compatible baseline model/runtime pair.

## Open questions
- Which Linux distro/version is the host? (Needed to pin package install instructions and GPU driver notes.)
- Which LAN auth mode should be default for opt-in profile (static token vs reverse proxy auth)?
- Which first model family should be baseline for smoke serving (chat-first vs coding-first)?

## Amendments
<!-- Append-only. Never edit or delete an existing entry. -->

- 2026-07-23 — Execution on this host could not run a live container health/sample-response verification because Docker daemon access is unavailable in-session. Delivered Docker baseline + OpenAI-compatible endpoint path + smoke benchmark template and script with placeholders, preserving localhost default and LAN-safe opt-in guidance.
- 2026-07-24T04:09:13Z — by engineering-manager, approved by pm-orchestrator: Director confirmed generator-first workflow; Sprint 1 closure now requires aligning legacy hand-authored artifacts to generated-template compatibility mode.

---
task_ref: TASK-0011
category: routine
status: approved
created: 2026-07-26
created_by: pm-orchestrator
approved_by: pm-orchestrator
approved_at: 2026-07-26T00:03:33Z
---

## Objective
Apply the Director's ESC-0002 decision that this project is licensed Apache-2.0: add the full LICENSE text at the repository root, correct pyproject.toml license metadata (PEP 621) and drop the Private :: Do Not Upload classifier, sweep README/docs/templates/scripts for now-false 'private, not distributed, no license' language, and make every generated workspace state explicitly that its output belongs to the operator rather than to this project. Verifiable when scripts/ci.sh passes, the golden fixture is regenerated with a reviewed diff, and no repository text still claims the project is private or unlicensed.

## Todos
- [x] Add LICENSE at the repository root with the complete, unmodified Apache License 2.0 text and a filled copyright line. AC: file exists, contains the Appendix boilerplate with the copyright line filled, and its body byte-matches the canonical Apache-2.0 text.
- [x] Replace pyproject.toml lines 10-15 with a PEP 621 license declaration (license = { file = "LICENSE" } or SPDX expression) plus license-files, and remove the 'Private :: Do Not Upload' classifier. AC: 'python3 -m build'/metadata generation is not required, but 'python3 -m pip check' and scripts/ci.sh pass and no 'Private ::' string remains in pyproject.toml.
- [x] Sweep README.md, docs/ (excluding docs/runtime-decision-phase-r.md), templates/, scripts/ and AGENTS.md/CLAUDE.md for 'private / personal use / not distributed / no license' wording that now contradicts a licensed clone-and-run product, and correct it without rewriting unrelated prose. AC: grep for the sweep terms returns only legitimate uses (private IP ranges, .pm-harness project isolation) and historical CHANGELOG entries superseded by a new entry.
- [x] State the generated-workspace license position in the emitted templates: output produced by the generator belongs to the operator, not to this project. AC: templates/chat/README.md.j2 (and runbook if warranted) carries an explicit, operator-readable statement; regenerating a workspace reproduces it.
- [x] Regenerate the golden fixture tests/golden/chat-ornith-medium-localhost/ with scripts/update_golden_fixture.py and review the diff line by line. AC: 'python3 scripts/update_golden_fixture.py --check' exits 0 and the diff contains only the intended licensing additions.
- [x] Add the CHANGELOG.md [Unreleased] entry for TASK-0011 under Added/Changed superseding the stale TASK-0007 'private, unlicensed' Security bullet. AC: 'harness.py changelog check --task TASK-0011' exits 0.
- [x] Run the full Gate 1 suite: scripts/ci.sh, harness.py validate, wiki check, plan check TASK-0011. AC: every command exits 0 with captured output.

## Gates
<!-- Exact Gate 1 commands (standards/GATES.md) this task must pass. -->

## Risks
<!-- What could go wrong; flag irreversible steps explicitly. -->

## Open questions
<!-- Unknowns that may become an escalation (SPEC §4) or an amendment. -->

## Amendments
<!-- Append-only. Never edit or delete an existing entry. -->

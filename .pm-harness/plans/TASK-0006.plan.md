---
task_ref: TASK-0006
category: complex
status: approved
created: 2026-07-24
created_by: product-manager
approved_by: pm-orchestrator
approved_at: 2026-07-24T15:37:26Z
---

## Objective
Produce `audits/TASK-0006-product-acceptance-audit.md` as an independent, evidence-backed reconciliation of the Director-approved scope and the closed deliverables from TASK-0001 through TASK-0005, distinguish delivered acceptance from roadmap-only or runtime-unverified work, correct every confirmed product-owned documentation/wiki/changelog defect without expanding scope, and give every remaining engineering or Director-level gap one traceable owner and disposition; acceptance is proven by no open `fix-now` product/documentation finding and by the exact gates below.

## Todos
- [x] Build the approved-scope register from the kickoff record, the PM Orchestrator memory notes, and the approved TASK-0001 through TASK-0005 plans. **AC:** `audits/TASK-0006-product-acceptance-audit.md` contains a source-cited row for every approved plan todo and every incorporated Director preference, with fields `requirement`, `source`, `delivery_task`, `expected_evidence`, and `scope_class` (`delivered`, `roadmap`, or `deferred`); `for id in TASK-0001 TASK-0002 TASK-0003 TASK-0004 TASK-0005; do grep -q "$id" audits/TASK-0006-product-acceptance-audit.md; done` exits 0.
- [x] Reconcile each closed task's manifest, approved plan, declared artifact, changelog reference, and current repository evidence without changing historical state. **AC:** the audit contains one TASK-0001 through TASK-0005 reconciliation table whose rows record manifest status, plan status, artifact path/existence, checksum match or mismatch, changelog evidence, and a literal evidence path/command; every mismatch is assigned a finding ID and no closed manifest, approved plan, or historical checksum is rewritten.
- [x] Independently verify the observable product behavior claimed by the five deliverables. **AC:** the audit records the command, exit code, and concise result for `python3 -m unittest`, generator help/list/matrix commands, a fresh localhost generate/validate flow, and the guarded LAN negative path; live Docker/model-serving claims are marked `verified`, `failed`, or `not-run` with prerequisites and are never inferred from static generation tests or matrix `GO`.
- [x] Audit the human-facing documentation and governance layer against actual CLI behavior and approved scope. **AC:** README.md, `docs/`, CHANGELOG.md, `.pm-harness/wiki/INDEX.md`, and affected wiki pages are covered by findings for canonical generated-script working-directory behavior, repository-root versus generated-workspace model-path resolution, actual versus advertised validation/LAN enforcement, destructive `--force` guidance, stale generated evidence, missing wiki ingestion, unsupported capability claims, and post-closure/unmapped work; each finding has `severity`, `evidence`, `scope_owner`, `disposition` (`fix-now`, `TASK-0007`, `follow-up`, `accepted`, or `Director-decision`), and `status`.
- [x] Resolve every confirmed product-owned `fix-now` finding while preserving the approved generator-first direction, and route code/security/runtime/operations defects to TASK-0007 instead of editing engineering-owned behavior at product-manager level. **AC:** modified product documentation matches current CLI/test evidence; `! rg -n 'disposition: fix-now.*status: open|status: open.*disposition: fix-now' audits/TASK-0006-product-acceptance-audit.md` exits 0; every TASK-0007 handoff has a reproduction, expected/actual result, and user-visible acceptance criterion; Coding/RAG/Vision and acceleration work is reported as pending roadmap unless a closed task explicitly promised its implementation.
- [x] Compile every materially changed product source into the LLM Wiki and close release documentation when observable behavior or documented state changes. **AC:** `.pm-harness/wiki/INDEX.md` has non-empty Pages and Sources sections covering the generator workflow, current capability status, security posture, and accepted decisions; contradictions retain both citations; `python3 .pm-harness/bin/harness.py wiki check` exits 0; and, when behavior/documented state changed, CHANGELOG.md has one user-facing TASK-0006 entry under `[Unreleased]`, the PR template is complete, and `python3 .pm-harness/bin/harness.py changelog check --task TASK-0006` exits 0.
- [x] Run the complete acceptance gate set and finalize audit evidence after TASK-0007 remediation evidence is available. **AC:** the audit records literal exit codes for every command under `## Gates`, `python3 .pm-harness/bin/harness.py plan check TASK-0006` exits 0 after all todos are checked, no finding lacks evidence/owner/disposition/status, and the final recommendation explicitly separates accepted deliverables, accepted-with-limitation evidence, resolved defects, pending approved-roadmap work, and scope changes requiring Director approval.

## Gates
- `python3 -m unittest`
- `python3 -m ai_server_generator --help`
- `python3 -m ai_server_generator list profiles`
- `python3 -m ai_server_generator list setups`
- `python3 -m ai_server_generator list models`
- `python3 -m ai_server_generator matrix --preset ornith-9b --profile medium --access localhost`
- `python3 -m ai_server_generator generate --preset ornith-9b --profile medium --access localhost --out generated/task-0006-acceptance --force`
- `python3 -m ai_server_generator validate generated/task-0006-acceptance`
- `python3 -m ai_server_generator generate --preset devstral-small-v25.07 --profile medium --access lan --out generated/task-0006-invalid-lan --dry-run` must exit non-zero
- `python3 .pm-harness/bin/harness.py validate`
- `python3 .pm-harness/bin/harness.py wiki check`
- `python3 .pm-harness/bin/harness.py plan check TASK-0006`
- `python3 .pm-harness/bin/harness.py changelog check --task TASK-0006` when observable behavior or documented state changes

## Risks
- The roadmap describes future Coding, RAG, Vision, acceleration, and multi-model work that TASK-0001 through TASK-0005 did not all commit to implement. Treat roadmap-only items as pending work, not closed-task defects; reverse any classification that cannot cite an explicit approved acceptance criterion.
- Static generator tests do not prove a live Docker/model-serving runtime. Record unavailable prerequisites as `not-run`, retain the runtime limitation, and never upgrade static evidence or placeholder benchmarks into a runtime pass.
- Findings may expose engineering-owned defects outside the product-manager's authority. Preserve their reproduction evidence, route them to TASK-0007 through the PM Orchestrator, and do not alter source/tests under this product acceptance task.
- Historical manifests, approved plans, checksums, and append-only records may contain stale or contradictory claims. Do not rewrite history; report the discrepancy and use a new governed task or amendment.
- Documentation remediation can accidentally broaden support claims or erase useful compatibility guidance. Review each documentation diff against CLI help/tests and roll back only TASK-0006 changes whose claims cannot be mechanically supported.
- Generated acceptance workspaces can overwrite local operator data. Resolve and inspect the dedicated `generated/task-0006-*` targets before `--force`; never touch another generated directory.

## Open questions
- No question blocks plan approval. During execution, only a conflict that would reclassify roadmap/deferred work as a current delivery commitment, change the approved scope, or accept a high-risk unresolved defect is escalated through the PM Orchestrator for Director resolution.

## Amendments
<!-- Append-only after approval. Never rewrite an approved plan. -->

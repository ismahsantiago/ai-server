# Retro — 2026-07-28

## Director input

—

## Improvement proposals (≥1 per manager)

### Engineering Manager (@engineering-manager)

1) **Workflow:** auto-sync `teams/*/plan.md` “Active/Next/Blocked” sections from `.pm-harness/state/*.json`

- **Classification (§6):** workflow
- **Problem observed:** multiple standup/showcase cycles show `teams/*/plan.md` entries whose “Active/Blocked/Next” labels no longer match the authoritative current `.pm-harness/state` (everything is `closed`, while plan snapshots still contain older “Active” items).
- **Proposed change:** before generating any `standup`/`showcase`/`retro`, run a deterministic read-only reconciliation pass:
  - parse `teams/{engineering-manager,product-manager}/plan.md`
  - rebuild their “Active/Next/Blocked” sections strictly from current `.pm-harness/state/*.json` statuses
  - add a `last_synced:` date line so future reviews can tell if a snapshot is stale.
- **Expected impact:** reduces confusion, prevents contradictory summaries, and keeps ceremony minutes aligned with execution reality.

### Product Manager (@product-manager)

1) **Prompt/quality rule:** in ceremonies, explicitly label `teams/*/plan.md` as “context snapshot (may be stale)” and prefer `.pm-harness/state` as source of truth

- **Classification (§6):** prompt
- **Problem observed:** ceremony minutes can look inconsistent when the plan snapshot contains historical labels.
- **Proposed change:** update the root-orchestrator standup/showcase/retro prompt templates/minutes format to:
  - always include a short “source of truth” sentence
  - explicitly state whether `teams/*/plan.md` was stale and whether reconciliation was applied.
- **Expected impact:** keeps authority clear, avoids “phantom work” impressions, and makes governance narratives consistent.

## Self-improvement cycle feed (§6)

Proposals above are recorded as **pending patch ideas**. They are **not applied** yet; they require Director approval before any SKILL.md/workflow edits.

1. **Proposed patch target:** pm-orchestrator ceremony-generation workflow (pre-pass plan snapshot reconciliation)
2. **Proposed patch target:** pm-orchestrator ceremony minutes/prompt templates (explicit “plan.md may be stale” labeling)

## Director decision needed

- Approve or defer the two proposal items above.

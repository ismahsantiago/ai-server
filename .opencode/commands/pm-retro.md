---
description: Run the retro ceremony (improvement proposals feeding the self-improvement cycle)
agent: pm-orchestrator
---
Run the `retro` ceremony exactly per `.pm-harness/HARNESS-SPEC.md` §5: collect
at least one improvement proposal per manager, feed the self-improvement cycle
(§6), and write `ceremonies/{YYYY-MM-DD}-retro.md` plus a `state/CER-*.json`
manifest via `python3 .pm-harness/bin/harness.py`.

Director input: $ARGUMENTS

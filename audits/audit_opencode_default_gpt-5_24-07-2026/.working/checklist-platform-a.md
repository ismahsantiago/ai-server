- [ ] **OPS-001 — Crítica.** Ubicaciones: `ai_server_generator/cli.py:288-296`, `ai_server_generator/cli.py:298-304`, `templates/chat/docker-compose.yml.j2:15-18`, `templates/chat/docker-compose.yml.j2:36-38`, `ai_server_generator/validator.py:51-53`, `manifests/chat.json:7-19`, `generated/ornith-medium-localhost/docker-compose.yml:13-15` y `generated/ornith-medium-localhost/docker-compose.yml:31-33`. Acción: definir un único contrato de montaje del modelo; resolver y renderizar por separado `host_model_path` y `container_model_path`, exigir antes de arrancar que el archivo host exista, sea regular y legible, y añadir una prueba que inspeccione la resolución real del bind mount de Compose.
- [ ] **COD-001 — Alta.** Ubicaciones: `ai_server_generator/render.py:39-53` y `ai_server_generator/render.py:172-178`. Acción: limitar toda salida a un descendiente estricto de `PROJECT_ROOT/generated`, rechazar symlinks en cualquier componente del destino y añadir pruebas negativas de `--force` para `.git`, `docs`, `models`, `scripts`, `audits` y rutas con symlink.
- [ ] **COD-002 — Alta.** Ubicaciones: `templates/chat/manifest.json.j2:7-20`, `templates/chat/env.j2:11-14`, `templates/chat/docker-compose.yml.j2:15-35` y `ai_server_generator/render.py:100-127`. Acción: construir el manifest con `json.dumps` sobre un objeto, validar paths y CIDR con parsers dedicados, serializar YAML y `.env` con quoting contextual seguro y cubrir comillas, saltos de línea, `:`, `#`, `${...}` y Unicode.
- [ ] **COD-003 — Alta.** Ubicaciones: `ai_server_generator/render.py:112`, `ai_server_generator/render.py:172-178`, `ai_server_generator/render.py:185-191` y `templates/chat/manifest.json.j2:6`. Acción: renderizar y validar en un directorio temporal hermano, hacer la sustitución mediante rename atómico conservando un backup recuperable, volver opcional o no canónico el timestamp, registrar un hash de inputs/plantillas y probar regeneración determinista.
- [ ] **OPS-002 — Alta.** Ubicaciones: `ai_server_generator/render.py:96-111`, `templates/chat/env.j2:11-14`, `templates/chat/docker-compose.yml.j2:6-35`, `templates/chat/scripts/start_serving.sh.j2:11-16` y `templates/chat/runbook.md.j2:19-26`. Acción: dejar de generar secretos, exigir en el arranque un secret file/store no vacío ni predeterminado, validar CIDR y aplicar el allowlist mediante un proxy o firewall verificable; si queda como requisito manual, mantener `NO-GO` hasta confirmar la regla con una comprobación automatizada.
- [ ] **OPS-003 — Alta.** Ubicaciones: `templates/chat/scripts/smoke_benchmark.sh.j2:10-44`, en particular `:20-22` y `:30-44`. Acción: leer de forma segura sólo la clave necesaria desde `.env`, usar un directorio temporal privado con `trap`, medir `%{time_total}`, exigir HTTP 200 y el JSON esperado, devolver código distinto de cero ante cualquier incumplimiento y separar el smoke estricto del benchmark diagnóstico.
- [ ] **COD-004 — Media.** Ubicaciones: `.gitignore:1`, `generated/chat-medium-localhost/manifest.json:1-22`, `manifests/chat.json:7-19` y `ai_server_generator/validator.py:10-29`. Acción: versionar fixtures dorados mínimos fuera de `generated/`, regenerarlos en CI y compararlos normalizando sólo metadatos volátiles, además de incorporar un comando `validate-all` o `drift-check` que recorra los workspaces locales y falle enumerando toda divergencia.

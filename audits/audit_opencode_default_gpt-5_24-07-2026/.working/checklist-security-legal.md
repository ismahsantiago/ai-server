- [ ] **SEC-001 — Crítica** — En `ai_server_generator/render.py:103-111`, `ai_server_generator/render.py:185-191`, `templates/chat/env.j2:11-14`, `templates/chat/scripts/start_serving.sh.j2:11-15` y `templates/chat/scripts/validate_host.sh.j2:16-24`, reemplazar el token fijo por un secreto criptográfico de al menos 32 bytes o por inyección desde un gestor de secretos, crear `.env` con modo `0600` y hacer que validación y arranque rechacen valores vacíos, débiles o iguales al placeholder.
- [ ] **SEC-002 — Alta** — En `templates/chat/docker-compose.yml.j2:7-9`, `templates/chat/docker-compose.yml.j2:32-35`, `templates/chat/env.j2:11-14`, `templates/chat/runbook.md.j2:21-23`, `templates/chat/README.md.j2:41-48` y `docs/lan-safe-runbook.md:8-26`, reemplazar la publicación HTTP directa en `0.0.0.0` por un proxy TLS autenticado delante de un backend no publicado, aplicar el CIDR de la allowlist en proxy/firewall y fallar cerrado si un preflight no demuestra el control; si no puede garantizarse, deshabilitar LAN.
- [ ] **SEC-003 — Alta** — En `ai_server_generator/cli.py:25-35`, `ai_server_generator/render.py:96-111`, `templates/chat/docker-compose.yml.j2:15-19`, `templates/chat/env.j2:11-14` y `templates/chat/manifest.json.j2:7-20`, reemplazar la interpolación textual por validación estricta de ruta/CIDR, rechazo de NUL/CR/LF y serialización segura con `json.dumps`, un emisor YAML y escaping dotenv; añadir regresiones de saltos de línea, comillas e inyección de propiedades Compose.
- [ ] **SEC-004 — Alta** — En `ai_server_generator/validator.py:63-76`, reemplazar las comprobaciones declarativas y coincidencias de texto por parseo estructural de JSON/YAML/dotenv, verificación cruzada de bind, CIDR, auth, secreto no-placeholder, TLS/proxy, firewall, privilegios y montajes, más `docker compose config`; añadir pruebas que muten individualmente cada control y exijan fallo cerrado.
- [ ] **SEC-005 — Media** — En `templates/chat/docker-compose.yml.j2:1-4`, `docker-compose.yml:4-7`, `requirements.txt:1`, `pyproject.toml:1-3` y `pyproject.toml:10-12`, reemplazar el tag mutable por una imagen fijada por digest y los rangos sin lock por dependencias bloqueadas con hashes; generar SBOM, añadir escaneo de imagen/dependencias en CI y documentar un proceso revisado de actualización y respuesta a vulnerabilidades.
- [ ] **SEC-006 — Media** — En `templates/chat/docker-compose.yml.j2:36-51` y `docker-compose.yml:31-46`, completar el aislamiento con UID/GID no-root verificado, `cap_drop: [ALL]`, `pids_limit`, límites de recursos, montaje de logs con permisos mínimos y red interna/backend cuando sea viable; convertir estos controles en invariantes estructurales y comprobarlos en runtime dentro de CI.
- [ ] **LEG-001 — Media** — En `pyproject.toml:5-12`, `templates/chat/docker-compose.yml.j2:1-4` y `ai_server_generator/presets.py:22-59`, añadir licencia y metadata del proyecto, `LICENSE`, `THIRD_PARTY_NOTICES`/SBOM y, por preset, fuente oficial, versión, licencia, restricciones y advertencia de provisión legítima; exigir nueva revisión legal al cambiar digest o catálogo antes de distribuir.

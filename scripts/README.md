# scripts/

Compatibility/example scripts for profile selection, serving, and smoke
benchmarking.

- Canonical workflow: run `matrix -> generate -> validate` and then use the
  generated workspace scripts under `generated/<preset-profile-access>/scripts/`.
- Legacy equivalents in this folder are retained for backward compatibility.
- Review script parameters before running on LAN-exposed setups.

# {{ setup }} setup ({{ profile }} profile, {{ access }} access)

Generated by ai-server-generator.

## Overview

- Runtime: `ghcr.io/ggerganov/llama.cpp:server`
- Endpoint: `POST /v1/chat/completions`
- Access mode: `{{ access }}`
{% if access == "lan" %}
- Network bind: `0.0.0.0:{{ host_port }}` (LAN, bearer-token protected)
{% else %}
- Network bind: `127.0.0.1:{{ host_port }}` (localhost only)
{% endif %}

## Resolved generation config

- Setup: `{{ resolved_setup }}`
- Profile: `{{ resolved_profile }}`
- Access: `{{ resolved_access }}`
- Preset alias: `{% if preset_alias %}{{ preset_alias }}{% else %}custom{% endif %}`
{% if preset_name %}- Preset name: `{{ preset_name }}` — {{ preset_summary }}
{% endif %}
- Capability tags: `{{ capability_tags }}`
- Memory guidance: {{ memory_guidance }}

## Quick start

```bash
{{ quick_validate_command }}
{{ quick_start_command }}
```

Health check:

```bash
docker compose ps
curl -sS http://127.0.0.1:{{ host_port }}/health
```

OpenAI-compatible smoke request:

```bash
{% if access == "lan" %}
curl -sS -X POST "http://127.0.0.1:{{ host_port }}/v1/chat/completions" \
  -H "Authorization: Bearer $API_BEARER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"model":"local","messages":[{"role":"user","content":"hello"}],"max_tokens":32}'
{% else %}
curl -sS -X POST "http://127.0.0.1:{{ host_port }}/v1/chat/completions" \
  -H "Content-Type: application/json" \
  -d '{"model":"local","messages":[{"role":"user","content":"hello"}],"max_tokens":32}'
{% endif %}
```

Smoke helper:

```bash
{{ quick_smoke_command }}
```

## Files

{% for f in required_files %}- `{{ f }}`
{% endfor %}

See `runbook.md` for operations and safety notes.

# Generated for setup={{ setup }} profile={{ profile }} access={{ access }}
PROFILE_NAME={{ profile }}
HOST_PORT={{ host_port }}
MODEL_PATH={{ container_model_path }}
CTX_SIZE={{ ctx_size }}
BATCH_SIZE={{ batch_size }}
THREADS={{ threads }}
N_PREDICT={{ n_predict }}
MEM_LIMIT={{ mem_limit }}
CPU_LIMIT={{ cpu_limit }}
{% if access == "lan" %}
# LAN access enabled. Set the bearer token secret before starting the stack.
API_BEARER_TOKEN={{ auth_token }}
LAN_ALLOWLIST={{ lan_allowlist }}
{% endif %}

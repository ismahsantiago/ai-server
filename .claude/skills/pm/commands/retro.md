---
description: Run the retro ceremony (improvement proposals feeding the self-improvement cycle)
argument-hint: [optional focus]
---

Read `.pm-harness/teams/pm-orchestrator/SKILL.md`, adopt the root-agent role,
and run the `retro` ceremony exactly per `.pm-harness/HARNESS-SPEC.md` §5:
collect at least one improvement proposal per manager, feed the
self-improvement cycle (§6), and write
`ceremonies/{YYYY-MM-DD}-retro.md` plus a `state/CER-*.json` manifest via
`python3 .pm-harness/bin/harness.py`.

Director input: $ARGUMENTS

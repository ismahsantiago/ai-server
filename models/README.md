# models/

Local model weights for Docker serving live here (for example, `.gguf` files).

- Keep this directory local-only.
- Do not commit proprietary or licensed model files.
- Baseline default expected by Docker: `models/placeholder.gguf` (replace with a real model).

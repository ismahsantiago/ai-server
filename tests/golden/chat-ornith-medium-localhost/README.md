# chat setup (medium profile, localhost access)

Generated by ai-server-generator.

## Overview

- Runtime: `ghcr.io/ggerganov/llama.cpp:server`
- Endpoint: `POST /v1/chat/completions`
- Access mode: `localhost` (the only supported generated exposure)
- Network bind: `127.0.0.1:8000` (localhost only)
- LAN status: disabled until an authenticated TLS gateway can enforce and
  mechanically verify a client allowlist

## Resolved generation config

- Setup: `chat`
- Profile: `medium`
- Access: `localhost`
- Preset alias: `ornith-9b`
- Preset name: `Ornith 1.0 (9B)` — agentic code specialist

- Capability tags: `code, agentic, tool-use`
- Memory guidance: Recommended: 7-9 GB RAM budget for stable local serving.

## Quick start

```bash
./scripts/validate.sh
./scripts/start.sh
```

Health check:

```bash
docker compose ps
curl -sS http://127.0.0.1:8000/health
```

OpenAI-compatible smoke request:

```bash
curl -sS -X POST "http://127.0.0.1:8000/v1/chat/completions" \
  -H "Content-Type: application/json" \
  -d '{"model":"local","messages":[{"role":"user","content":"hello"}],"max_tokens":32}'
```

Smoke helper:

```bash
./scripts/smoke.sh
```

## Files

- `docker-compose.yml`
- `.env`
- `manifest.json`
- `README.md`
- `runbook.md`
- `scripts/start.sh`
- `scripts/validate.sh`
- `scripts/smoke.sh`
- `scripts/stop.sh`
- `scripts/start_serving.sh`
- `scripts/smoke_benchmark.sh`
- `scripts/validate_host.sh`


## License

`ai-server` is licensed under Apache-2.0. That license covers the generator;
it does not cover what the generator produces. Everything in this workspace —
`docker-compose.yml`, `.env`, the scripts, this README, and `runbook.md` — was
generated for you and is yours to use, modify, and redistribute as you see
fit. This project claims no rights over it.

The container image and any model weights this workspace pulls or mounts are
not covered by that license either. They carry their own upstream licenses —
check those before you redistribute a workspace that includes them.

See `runbook.md` for operations and safety notes.

# profiles/

Legacy JSON profile examples retained for compatibility and reference.

- Canonical workflow is generator-first (`clone -> matrix -> generate -> validate -> start`).
- Prefer generated artifacts under `generated/<preset-profile-access>/`.
- Equivalent generated runtime values are recorded in each generated
  `manifest.json` / `runbook.md` and emitted config files.
